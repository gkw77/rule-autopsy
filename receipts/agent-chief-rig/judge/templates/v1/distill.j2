Translate today's preference-weight changes into ONE short human-readable policy line a user would recognize, format exactly:
- {rule} (learned {{ date }}, source: {stats})
Weight changes: {{ changes }}
Output the single line only.
